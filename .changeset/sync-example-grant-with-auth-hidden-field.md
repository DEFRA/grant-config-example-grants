---
"grant-config-example-grants": patch
---

Allow null for hiddenField in example-grant-with-auth submission schema (form declares `required: false` with no default), and sync gas.json with the PR #862 field set so the GAS-side schema matches the deployed form.
